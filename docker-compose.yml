# Build image
# `docker-compose -f docker-compose.yml build`
# Push to registry
# `docker-compose -f docker-compose.yml push`

version: '3.8'
services:
  app:
    image: registry.lts.harvard.edu/lts/hgl:0.0.15
    build:
      context: .
      dockerfile: Dockerfile
